<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             x:Class="Econtainer.Views.PlantSubsystemPage"
             mc:Ignorable="d"
             x:Name="ContainerListPage"
             BackgroundColor="#121212"
             Title="Container Overview">

    <CollectionView ItemsSource="{Binding Containers}"
                    SelectionMode="Single"
                    SelectionChanged="OnSelectionChanged">
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Frame BorderColor="Gray" BackgroundColor="#333333" Padding="10" Margin="5">
                    <StackLayout>
                        <Label Text="{Binding Name}" FontSize="Medium" TextColor="White"/>
                        <Label Text="{Binding Temperature, StringFormat='Temp: {0:F1}°C'}" TextColor="LightGray"/>
                        <Label Text="{Binding Humidity, StringFormat='Humidity: {0}%'}" TextColor="LightGray"/>
                    </StackLayout>
                </Frame>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>
